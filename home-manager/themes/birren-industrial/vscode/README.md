# Birren Industrial for VS Code

Local color theme extension providing `Birren Industrial Dark` and `Birren Industrial Light`.

The extension is generated from the shared Nix-managed palette and installed through Home Manager. Select a theme
with **Preferences: Color Theme** (`Cmd+K Cmd+T` on macOS).
